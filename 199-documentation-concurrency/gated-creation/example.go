func Serve(queue chan *Request) {
    for req := range queue {
        sem <- 1 //  ขอโควตาก่อน! ถ้าเต็มจะหยุดรอตรงนี้ ไม่สร้าง Goroutine ใหม่
        go func() {
            process(req)
            <-sem
        }()
    }
}

การทำงาน: ย้าย sem <- 1 ออกมาไว้นอก Goroutine ในลูป Serve

ผลลัพธ์: ถ้าโควตาเต็ม 100 งานแล้ว ลูป Serve จะถูกบล็อกหยุดอยู่ตรง sem <- 1 ทันที และจะไม่สร้าง Goroutine ใหม่จนกว่าจะมีงานเก่าทำเสร็จ ช่วยเซฟ RAM ได้สมบูรณ์แบบ

ข้อควรระวัง (ตามที่เนื้อหาหมายเหตุไว้): ใน Go เวอร์ชั่นเก่ากว่า 1.22 
ตัวแปร req ในลูปสโคปจะถูกใช้ร่วมกัน ต้องระวังเรื่อง Closure 
แต่ถ้าใช้ Go 1.22 ขึ้นไป ปัญหานี้ได้รับการแก้ไขในระดับตัวภาษาแล้วครับ
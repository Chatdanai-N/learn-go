package acdc

import "fmt"

func ExampleSum() {
	fmt.Println(Sum(2, 3))
	// Output:
	// 7
}

/*
ตัวอย่างโค้ดที่คุณให้มาเป็น Example Test ใน Go
ซึ่งถูกออกแบบมาเพื่อใช้เป็นทั้ง ตัวอย่างการใช้งาน (Documentation)
และ การทดสอบโค้ด (Test) ในตัวเดียวกัน


การทำงานตามปกติ (Execution Completed)
go test ./..
ให้ค้นหาและรันเทสต์ทุก package ในโฟลเดอร์ปัจจุบันและโฟลเดอร์ย่อยทั้งหมด



1. Examples คืออะไร และมีประโยชน์อย่างไร?

	เป็นทั้ง Document และ Test ในตัวเดียวกัน: Examples
	คือโค้ดตัวอย่างที่แสดงผลบนหน้าเอกสาร (Godoc / pkg.go.dev)
	และจะถูกรันเพื่อทดสอบอัตโนมัติเมื่อเราพิมพ์ go test

	เอกสารไม่ล้าสมัย (Never Out of Date): เพราะทุกครั้งที่มีการรันเทสต์
	ตัวอย่างโค้ดจะถูกตรวจสอบเสมอ
	ถ้ามีการแก้โค้ดหลักแล้วตัวอย่างทำงานไม่ตรง เทสต์จะ FAIL ทันที

2. การทำงานของคอมเมนต์ // Output:
	วิธีเช็คผลลัพธ์: Go test runner จะจับข้อความที่พิมพ์ออกทาง
	Standard Output (เช่น fmt.Println)
	แล้วนำไปเปรียบเทียบกับข้อความหลังคอมเมนต์ // Output: หากตรงกัน เทสต์จะ PASS

	ไม่มี // Output:: หากฟังก์ชันขึ้นต้นด้วย Example
	// แต่ไม่มีคอมเมนต์ // Output: ตัว Go จะทำการ Compile
	// โค้ดอย่างเดียวแต่จะไม่สั่ง Execute
	// (เหมาะกับโค้ดตัวอย่างที่รันจริงในเทสต์ไม่ได้ เช่น โค้ดที่ต้องเชื่อมต่อเครือข่าย หรือติดต่อฐานข้อมูล)

3. กฎการตั้งชื่อฟังก์ชัน (Naming Conventions)
	Godoc จะนำโค้ดตัวอย่างไปแปะไว้ในหน้าเอกสารให้อัตโนมัติ โดยอ้างอิงจากชื่อฟังก์ชัน:

	การตั้งชื่อ
	func Example() แสดงเป็นตัวอย่างการใช้งานในระดับภาพรวมของ Package
	func ExampleFoo() แสดงเป็นตัวอย่างของ ฟังก์ชันหรือ Type ชื่อ Foo
	func ExampleBar_Qux() แสดงเป็นตัวอย่างของ Method ชื่อ Qux ของ structural type Bar

	หากมีหลายตัวอย่างสำหรับ Identifier เดียวกัน: ให้ใช้เครื่องหมาย _ ต่อด้วยตัวพิมพ์เล็ก
	เช่น func ExampleString_second() หรือ func ExampleString_third()

4. ตัวอย่างขนาดใหญ่ (Whole File Examples)

	สำหรับกรณีที่ต้องแสดงตัวอย่างการใช้งานซับซ้อน
	(เช่น ต้องมี struct หรือต้อง implement interface อย่าง sort.Interface
	ซึ่งไม่สามารถประกาศภายในฟังก์ชันได้):

	เงื่อนไข: สร้างไฟล์ _test.go ขึ้นมา 1 ไฟล์ โดยในไฟล์นั้นต้องมี func Example()
	เพียงฟังก์ชันเดียว และไม่มี Test หรือ Benchmark อื่นเลย

	ผลลัพธ์: Godoc จะดึงโค้ด ทั้งไฟล์ ไปแสดงเป็นตัวอย่างในหน้าเอกสารทันที

การเขียน Example เป็นแนวปฏิบัติที่ดีที่สุด (Idiomatic Go) ในการเขียนเอกสารกำกับไลบรารี
เพราะนอกจากจะช่วยให้ผู้ใช้อ่านและรันโค้ดตัวอย่างบนหน้าเว็บ (เช่น pkg.go.dev) ได้ง่ายแล้ว
ยังรับประกันได้ว่าเอกสารประกอบโค้ดจะถูกต้องและใช้งานได้จริงเสมอ

*/
